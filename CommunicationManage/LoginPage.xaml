<Page
    x:Class="CommunicationManage.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CommunicationManage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Windows10FallCreatorsUpdate="using:Windows.UI.Xaml.Media"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <AcrylicBrush x:Name="CMapUwpAcrylicBrush"
              BackgroundSource="HostBackdrop"
              TintColor="#FFE4E4E4"
              TintOpacity="0.05"
              FallbackColor="{StaticResource SystemChromeWhiteColor}"/>
        </Grid.Background>
        <Frame x:Name="loginpage"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="账号:" Margin="0,0,10,10"/>
                <TextBox Name="account" Margin="10,0,0,10" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="密码:" Margin="0,0,10,10"/>
                <PasswordBox Name="password" Margin="10,0,0,10" Width="200" />
            </StackPanel>
            <Button Name="loginbutton" Margin="0,10,0,0" HorizontalAlignment="Center" FontSize="16" Content="登录" Click="loginbutton_Click"/>
        </StackPanel>
    </Grid>
</Page>
